namespace TradingJournal.Data.Models
{
    public enum TradeDirection { Long, Short }
    public enum AdjustmentType { StopLoss, TakeProfit, PartialExit }
    public enum ThemeMode { Light, Dark, System }
}
